<Window 
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="clr-namespace:Coral_Launcher.Views"
    xmlns:av="http://schemas.microsoft.com/expression/blend/2008" 
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
    mc:Ignorable="av" 
    x:Class="Coral_Launcher.MainWindow"
    Title="Coral Launcher"
    Height="660" Width="1025"
    ResizeMode="CanMinimize"
    WindowStyle="SingleBorderWindow"
    WindowStartupLocation="CenterScreen"
    Background="#1E1E1E"
    Icon="/Icon.ico">
    

    <Window.Resources>
        
        <Style x:Key="GlowButtonStyle" TargetType="{x:Type Button}">
            <Setter Property="Background" Value="#333333"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Width" Value="150"/>
            <Setter Property="Height" Value="40"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Padding" Value="5,0"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type Button}">
                        <Border Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}" 
                                CornerRadius="10">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>

            <Style.Triggers>
                
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="BorderBrush" Value="White"/>
                    <Setter Property="BorderThickness" Value="2"/>
                </Trigger>
                
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Background" Value="#444444"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Grid>
        
        <Border Background="#252526" Height="60" HorizontalAlignment="Stretch" VerticalAlignment="Top">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                
                <Button Content="Play Coral" Style="{StaticResource GlowButtonStyle}" 
                        Click="HomeButton_Click" HorizontalAlignment="Left" VerticalAlignment="Center" Margin="10,0" Grid.Column="0"/>

                
                <Button Content="Settings" Style="{StaticResource GlowButtonStyle}" 
                    Click="SettingsButton_Click" HorizontalAlignment="Left" VerticalAlignment="Center" Margin="352,0,0,0" Grid.Column="1"/>

                
            </Grid>
        </Border>

        
        <ContentControl x:Name="MainContent" Margin="0,60,0,0"
                HorizontalAlignment="Stretch"
                VerticalAlignment="Stretch"/>
    </Grid>
</Window>
